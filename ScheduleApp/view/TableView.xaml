<UserControl x:Class="ScheduleApp.view.TableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScheduleApp.view"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <ComboBox Grid.Row="0" Name="selectCollection" SelectionChanged="dataGrid_SelectionChanged">

            <ComboBoxItem Content="Classrooms"/>
            <ComboBoxItem Content="Departments"/>
            <ComboBoxItem Content="Classroom software"/>
            <ComboBoxItem Content="Subjects"/>
        </ComboBox>
        <DataGrid Grid.Row="1" Name="dataGrid" AutoGeneratingColumn="dataGrid_AutoGeneratingColumn" IsReadOnly="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        </DataGrid>
    </Grid>
</UserControl>
